"use client";
import { useRouter } from "next/navigation";
import { toast } from "sonner";
import { saveTournamentMatch } from "@/actions/community-admin.actions";

export default function MatchManager({tournament}){const router=useRouter();async function submit(e){e.preventDefault();const r=await saveTournamentMatch({...Object.fromEntries(new FormData(e.currentTarget)),tournamentId:tournament.id});r.error?toast.error(r.error):(toast.success("Match saved"),e.currentTarget.reset(),router.refresh());}return <div className="space-y-6"><form onSubmit={submit} className="bg-white border rounded-xl p-6 grid md:grid-cols-2 gap-3"><h2 className="font-bold text-xl md:col-span-2">Add fixture or result</h2><select name="homeTeamId" required className="border rounded p-3"><option value="">Home team</option>{tournament.teams.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select><select name="awayTeamId" required className="border rounded p-3"><option value="">Away team</option>{tournament.teams.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select><input name="scheduledAt" type="datetime-local" className="border rounded p-3"/><input name="venue" placeholder="Venue" className="border rounded p-3"/><select name="status" className="border rounded p-3"><option value="scheduled">Scheduled</option><option value="ongoing">Ongoing</option><option value="completed">Completed</option><option value="cancelled">Cancelled</option></select><select name="winningTeamId" className="border rounded p-3"><option value="">No winner / not decided</option>{tournament.teams.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select><input name="homeScore" placeholder="Home score" className="border rounded p-3"/><input name="awayScore" placeholder="Away score" className="border rounded p-3"/><input name="resultNote" placeholder="Result note" className="border rounded p-3 md:col-span-2"/><button className="bg-primary text-white p-3 rounded md:col-span-2">Save match</button></form><div className="bg-white border rounded-xl divide-y">{tournament.matches.map(m=><div key={m.id} className="p-4 flex justify-between"><div><p className="font-semibold">{m.homeTeam.name} vs {m.awayTeam.name}</p><p className="text-sm text-gray-500">{m.venue||"Venue TBA"} · {m.status}</p></div><p className="font-bold">{m.homeScore||"–"} : {m.awayScore||"–"}</p></div>)}{!tournament.matches.length&&<p className="p-6 text-gray-500">No fixtures yet.</p>}</div></div>}
