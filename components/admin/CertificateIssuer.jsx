"use client";
import { toast } from "sonner";import { issueCertificate } from "@/actions/community-admin.actions";
export default function CertificateIssuer({users,events}){return <form className="bg-white border rounded-xl p-6 grid md:grid-cols-2 gap-3" onSubmit={async e=>{e.preventDefault();const r=await issueCertificate(Object.fromEntries(new FormData(e.currentTarget)));r.error?toast.error(r.error):(toast.success("Certificate issued"),e.currentTarget.reset());}}><select name="userId" required className="border rounded p-3"><option value="">Select member</option>{users.map(u=><option key={u.id} value={u.id}>{u.firstName} {u.middleName} {u.surname}</option>)}</select><select name="eventId" className="border rounded p-3"><option value="">General certificate</option>{events.map(e=><option key={e.id} value={e.id}>{e.title}</option>)}</select><input name="title" required placeholder="Certificate title" className="border rounded p-3"/><input name="fileUrl" placeholder="PDF URL (optional)" className="border rounded p-3"/><button className="bg-primary text-white rounded p-3 md:col-span-2">Issue certificate</button></form>}
