"use client";import { useRouter } from "next/navigation";import { toast } from "sonner";import { updateVolunteerApplication } from "@/actions/community-admin.actions";
export default function VolunteerReview({id,status}){const router=useRouter();async function set(next){const hours=next==="completed"?window.prompt("Volunteer hours completed", "1"):null;const r=await updateVolunteerApplication(id,next,hours);r.error?toast.error(r.error):(toast.success("Updated"),router.refresh());}return <div className="flex gap-2 text-sm">{status==="applied"&&<><button onClick={()=>set("accepted")} className="text-green-700">Accept</button><button onClick={()=>set("declined")} className="text-red-700">Decline</button></>}{status==="accepted"&&<button onClick={()=>set("completed")} className="text-primary">Complete & log hours</button>}{!["applied","accepted"].includes(status)&&<span className="capitalize">{status}</span>}</div>}
