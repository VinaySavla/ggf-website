"use client";
import { useRouter } from "next/navigation";
import { toast } from "sonner";
import { saveStatDefinition } from "@/actions/community-admin.actions";

export default function StatDefinitions({sports}){const router=useRouter();return <section className="bg-white rounded-xl border p-6 mt-8"><h2 className="text-xl font-bold">Structured statistic fields</h2><p className="text-sm text-gray-500 mt-1">Define consistent fields used for entry, comparison and leaderboards.</p><div className="grid lg:grid-cols-2 gap-5 mt-5">{sports.map(sport=><div key={sport.id} className="border rounded-lg p-4"><h3 className="font-semibold">{sport.name}</h3><div className="flex flex-wrap gap-2 my-3">{sport.statDefinitions.map(d=><span key={d.id} className="bg-gray-100 px-2 py-1 text-xs rounded">{d.label} ({d.valueType})</span>)}</div><form className="flex gap-2" onSubmit={async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget));const r=await saveStatDefinition({...d,sportId:sport.id});r.error?toast.error(r.error):(toast.success("Field saved"),e.currentTarget.reset(),router.refresh());}}><input name="label" required placeholder="e.g. Runs" className="border rounded p-2 min-w-0 flex-1"/><select name="valueType" className="border rounded"><option value="number">Number</option><option value="text">Text</option><option value="duration">Duration</option></select><button className="bg-primary text-white px-3 rounded">Add</button></form></div>)}</div></section>}
