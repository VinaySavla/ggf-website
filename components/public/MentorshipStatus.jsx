"use client";
import { useRouter } from "next/navigation";import { toast } from "sonner";import { updateMentorshipStatus } from "@/actions/member.actions";
export default function MentorshipStatus({id,incoming,status}){const router=useRouter();async function set(next){const r=await updateMentorshipStatus(id,next);r.error?toast.error(r.error):(toast.success("Request updated"),router.refresh());}if(status!=="requested")return <span className="text-sm capitalize">{status}</span>;return <div className="flex gap-2">{incoming&&<><button onClick={()=>set("accepted")} className="text-green-700">Accept</button><button onClick={()=>set("declined")} className="text-red-700">Decline</button></>} {!incoming&&<button onClick={()=>set("cancelled")} className="text-red-700">Cancel</button>}</div>}
